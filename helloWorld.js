const greet = "hello world";
console.log(greet);
// we know we can run a js program by node using node and then file nam e

// node js removed all the browser features like , window , fetch etc 

// so we can run js code inside  node without browser engine 